# Optional: backend base URL when not using same-origin /api (e.g. nginx proxy or Ingress).
# Leave empty for Docker Compose + Kubernetes Ingress path routing.
# Dev server proxies /api → http://localhost:5000 (see vite.config.js).
VITE_BACKEND_URL=
